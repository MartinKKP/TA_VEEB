<!DOCTYPE html>
<html lang="et">
<head>
  <meta charset="utf-8">
  <title>Pildigalerii – Martin Kullerkupp, VP</title>
  <link rel="stylesheet" href="/style/gallery.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 900px;
      margin: auto;
      background: #f9f9f9;
      color: #333;
    }
    header, nav {
      margin-bottom: 20px;
    }
    img.banner {
      width: 100%;
      height: auto;
    }
    nav ul {
      list-style: none;
      padding: 0;
    }
    nav li {
      display: inline;
      margin-right: 10px;
    }
    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 15px;
    }
    .gallery-item {
      background: white;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      padding: 5px;
      text-align: center;
    }
    .gallery img {
      width: 100%;
      border-radius: 5px;
    }
    .caption {
      font-size: 0.9em;
      color: #555;
      margin-top: 5px;
    }
  </style>
</head>
<body>
  <header>
    <img class="banner" src="/images/vp_banner_2025_TA.jpg" alt="kursuse bänner">
    <h1>Pildigalerii</h1>
    <p>Siin on kõik avalikud fotod.</p>
  </header>

  <nav>
    <ul>
      <li><a href="/">Avaleht</a></li>
      <li><a href="/galleryphotoupload">Lae pilt üles</a></li>
      <li><strong>Galerii</strong></li>
    </ul>
  </nav>

  <main>
    <% if (photos.length > 0) { %>
      <div class="gallery">
        <% photos.forEach(photo => { %>
          <div class="gallery-item">
            <img src="/gallery/normal/<%= photo.filename %>" alt="<%= photo.alttext || 'Foto' %>">
            <div class="caption"><%= photo.alttext || 'Foto' %></div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p>Galerii on tühi. Lae mõni pilt üles!</p>
    <% } %>
  </main>
</body>
</html>
